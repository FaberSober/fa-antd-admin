# fa-h5-demo-pages

H5 组件和交互示例模块，依赖 `fa-h5-base-pages` 和 `fa-h5-file-preview-pages`。用户可以从工作台进入 Demo 功能列表，再进入具体组件示例页面。

文件预览 Demo 默认使用 H5 内置样例；如果配置 `VITE_APP_H5_PREVIEW_DEMO_FILE_ID`，则使用配置的公共文件。

当前 default 和 demo 项目预设均启用本 Feature。
