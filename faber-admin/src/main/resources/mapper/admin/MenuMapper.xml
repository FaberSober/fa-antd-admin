<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.faber.admin.mapper.MenuMapper">

    <select id="selectMenuByAuthorityId" resultType="com.faber.admin.entity.Menu">
        select t.*
        from base_resource_authority ra
                 inner join base_menu t on ra.resource_id = t.id and t.del_state = 0
                and ra.authority_id = #{authorityId}
                and ra.authority_type = #{authorityType}
                and ra.resource_type = 'menu'
    </select>

    <select id="selectAuthorityMenuByUserId" resultType="com.faber.admin.entity.Menu">
        select distinct t.*
        from base_resource_authority ra
                 inner join base_menu t on ra.resource_id = t.id and t.del_state = 0
                and ra.authority_id in (
                    select group_id
                    from base_group_user
                    where del_state = 0 and user_id = #{userId}
                )
                and ra.authority_type = 'group'
                and ra.resource_type = 'menu'
        order by t.id
    </select>

    <select id="selectAuthoritySystemByUserId" resultType="com.faber.admin.entity.Menu">
        select distinct t.*
        from base_resource_authority ra
                 inner join base_menu t on ra.resource_id = t.id and t.del_state = 0
                and ra.authority_id in (
                    select group_id
                    from base_group_user
                    where del_state = 0 and user_id = #{userId}
                )
                and ra.authority_type = 'group'
                and ra.resource_type = 'menu'
        where t.parent_id = -1
    </select>

    <select id="findNotAttachedMenuIds" resultType="int">
        SELECT t.id
        FROM base_menu t
        WHERE t.del_state = 0
          AND t.parent_id != -1
          AND t.parent_id NOT IN (
            SELECT id FROM base_menu WHERE del_state = 0
            )
    </select>

    <update id="removeByMenuIds" parameterType="map">
        UPDATE base_menu
        SET del_state = '1',
            del_user = #{id},
            del_name  = #{name},
            del_host  = #{ip},
            del_time  = SYSDATE()
        WHERE id IN (${ids});
    </update>
</mapper>
