<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.faber.admin.mapper.GroupMapper">

    <select id="findByUserId" parameterType="map" resultType="com.faber.admin.entity.Group">
        SELECT * FROM base_group t WHERE t.del_state = 0 AND t.id IN (
            SELECT group_id FROM base_group_user WHERE user_id = #{userId}
        )
    </select>

</mapper>
